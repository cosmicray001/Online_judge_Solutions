#include <bits/stdc++.h>
#define le 102
using namespace std;
int n[le][le];
int main(){
  int n, r = 0, c = 0;
  string s;
  while(scanf("%d", &n) != EOF && n){
    getchar();
    getline(cin, s);
    for()
  }
}
