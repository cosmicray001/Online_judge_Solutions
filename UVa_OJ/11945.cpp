#include <bits/stdc++.h>
using namespace std;
int d;
void fnc(string s){
  if(d)
}
int main(){
  double sum, avg, c;
  int len, co = 0;
  for(scanf("%d", &len); len--; ){
    sum = 0.0;
    for(int i = 0; i < 12; scanf("%lf", &c), sum += c, i++);
    sum /= 12.0;
    d = 0;
    string s = "";
    s += (int) sum;
    printf("%d $", ++co);
    fnc(s);
    printf("%.2lf\n", sum - (int)sum);
  }
  return 0;
}
