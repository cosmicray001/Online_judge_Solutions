#include <bits/stdc++.h>
#define le 22
using namespace std;
int n[le], m[le];
int main(){
  int len;
  cin >> len;
  for(int i = 0; i < len; scanf("%d", &n[i]), i++);
  for(int i = 0; i < len; scanf("%d", &m[i]), i++);
  int sum = -INT_MAX, x, y;
  for(int i = )
}
