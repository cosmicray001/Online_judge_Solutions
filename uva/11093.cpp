#include <bits/stdc++.h>
using namespace std;
int main(){
  int t, len, a, co = 0;
  for(scanf("%d", &t); t--; ){
    vector<int> v;
    vector<int> ve;
    scanf("%d", &len);
    for(int i = 0; i < len; scanf("%d", &a), v.push_back(a), i++);
    for(int i = 0; i < len; scanf("%d", &a), ve.push_back(a), i++);
    bool f;
    for(int i = 0; i < len; i++){

    }
  }
}
